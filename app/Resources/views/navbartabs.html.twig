
<div class="nav-content">
    <ul class="tabs tabs-transparent">
        {% for tab in tabs %}
            <li class="tab"><a href="{{ tab.href }}" {{ tab.active ? 'class="active"' }}>{{ tab.text }}</a></li>
        {% endfor %}
    </ul>
</div>

